# Optional. Classification and planning are deterministic and do not need a key.
# A key is only used later if you wire specialist adapters to live model runs.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
